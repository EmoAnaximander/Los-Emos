[server]
enableCORS = false
enableXsrfProtection = false
# respect Cloud Run’s proxy headers (important for the Host/Origin)
useForwardedHeaders = true
# allow your exact URL (you can keep the wildcard too if you like)
allowedOrigins = [
  "https://los-emos-karaoke-15879119077.us-west2.run.app"
]
# any long random string
cookieSecret = "this-is-a-long-random-string"
